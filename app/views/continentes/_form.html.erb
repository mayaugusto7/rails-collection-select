<%= form_with(model: continente) do |form| %>
  <% if continente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(continente.errors.count, "error") %> prohibited this continente from being saved:</h2>

      <ul>
        <% continente.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
